# Application
VITE_APP_NAME="Andressa Alana Gestao de Estetica"

# Supabase (Server)
# Use a URL no formato: https://<project-ref>.supabase.co
SUPABASE_URL=""
# Use uma chave de servidor (legacy service_role ou sb_secret) para as rotas /api.
SUPABASE_SERVICE_ROLE_KEY=""

# Supabase (Client Auth)
# Use a mesma URL do projeto no navegador.
VITE_SUPABASE_URL=""
# Chave publica para login do frontend (publishable ou anon).
VITE_SUPABASE_PUBLISHABLE_KEY=""
# Compatibilidade opcional com projetos antigos.
VITE_SUPABASE_ANON_KEY=""

# Optional: define o ano fiscal exibido no modulo de financas.
# Se vazio, usa o ano atual automaticamente.
FINANCE_YEAR=""
